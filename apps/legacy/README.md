# Fleet Control — monolito legacy (deprecado)

**No uses esta app en producción.** Mantenida solo para referencia o transición corta.

| Usa en su lugar | Comando |
|-----------------|---------|
| Trabajadores en ruta | `npm run start:worker` → [apps/worker](../worker/README.md) |
| Supervisión / mapa | `npm run start:admin` → [apps/admin](../admin/README.md) |

Package Android: `com.fleetcontrol.mobile` (mismo que el monolito original).

```bash
npm run start:legacy
```
